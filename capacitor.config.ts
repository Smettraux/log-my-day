import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.websauce.travellog',
  appName: 'Travel Log',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
